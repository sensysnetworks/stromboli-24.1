****** BITS EXAMPLE ******

This example demonstrates the use of a few basic services of rtai_bits in kernel
space.

To use it just type:

make
./run

Alternatively use your own insmod/rmmod after make.
