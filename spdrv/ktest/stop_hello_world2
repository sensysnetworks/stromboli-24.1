sync
if lsmod | grep "hello_world2">/dev/null ; then
    rmmod hello_world2
fi
if lsmod | grep "rtai_spdrv">/dev/null ; then
    rmmod rtai_spdrv
fi
if lsmod | grep "rtai_sched">/dev/null ; then
    rmmod rtai_sched
fi
if lsmod | grep "rt_mem_mgr">/dev/null ; then
    rmmod rt_mem_mgr
fi
if lsmod | grep "rtai">/dev/null ; then
    rmmod rtai
fi
